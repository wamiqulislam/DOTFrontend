<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DOTFrontend"
        tools:targetApi="31"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">
        <activity

            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.SignUpActivity" />
        <activity android:name=".activity.ChangeParcelActivity" />
        <activity android:name=".activity.SendParcelActivity" />
        <activity android:name=".activity.RegisterVehicleActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.RiderInterfaceActivity" />
        <activity android:name=".activity.CustomerInterfaceActivity" />
        <activity android:name=".activity.AdminInterfaceActivity" />
        <activity android:name=".activity.ViewParcelActivity" />
        <activity android:name=".activity.ChooseParcelActivity" />
        <activity android:name=".activity.FinishDeliveryActivity" />
        <activity android:name=".activity.ViewBatchesActivity" />
        <activity android:name=".activity.ViewParcelsActivity" />


    </application>

</manifest>